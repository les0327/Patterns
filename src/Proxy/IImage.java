package Proxy;

/**
 * Interface describe image
 *
 * @author Les
 * @version 3.0
 */
public interface IImage {

    /**
     * Load image
     *
     * @param imageName - name of image that we want to load
     */
    void loadImage(String imageName);
}
